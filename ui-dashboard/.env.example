# ⚠️ DEPRECATED - Environment variables are no longer used
# 
# Configuration is now managed exclusively through config.yaml
# Please edit config.yaml to configure the application
#
# Configuration hierarchy:
# 1. YAML config file (config.yaml) - HIGHEST PRIORITY
# 2. Hardcoded defaults in src/config/configLoader.js - LOWEST PRIORITY

