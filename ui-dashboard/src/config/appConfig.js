/**
 * Application Configuration
 * Centralized configuration for all projects and services
 */

import { loadedConfig } from './configLoader'

// Export the loaded configuration directly
export default loadedConfig
